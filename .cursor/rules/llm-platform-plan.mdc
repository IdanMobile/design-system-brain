---
description: Side project LLM platform — when user says "look in LLM-platform-plan", read AGENTS.md first and enter side-project mode
globs:
alwaysApply: false
---

# LLM Platform Plan (side project)

If the user mentions **LLM-platform-plan**, **look in LLM-platform-plan**, **platform plan**, or **side project plan**:

1. Read `docs/LLM-platform-plan/AGENTS.md` **immediately** (full file).
2. Follow `docs/LLM-platform-plan/GUIDELINES.md` and `docs/LLM-platform-plan/WORKFLOWS.md` as directed there.
3. Route to other docs in `docs/LLM-platform-plan/` only via AGENTS.md — do not improvise.
4. Do **not** follow `docs/ROADMAP.md`, lab fix loops, test console workflows, or `figma-renderer-until-pass` unless the user **explicitly** overrides (e.g. “ignore platform plan, fix the lab”).

This is a **standalone side project**, not the storybook-to-figma-lab roadmap.

Optional machine index: `docs/LLM-platform-plan/MANIFEST.yaml`.
