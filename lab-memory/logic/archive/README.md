# Logic spec archive (v1)

One-way archive from `pnpm specs:bootstrap-v2`. Tools read **`logic/specs/`** only — not this folder.

Do not link these files from Obsidian graph workflows; they are historical reference.
