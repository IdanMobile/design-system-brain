run until pass

Worst story: mui--showcase (0.11% global, fail, Figma emulator) · worst hotspot 2.01%
Compare: /Users/user/Downloads/storybook-to-figma-lab/figma-diffs/mui-showcase/regions/region-01-compare.png
Storybook: /Users/user/Downloads/storybook-to-figma-lab/figma-diffs/mui-showcase/storybook.png
Figma: /Users/user/Downloads/storybook-to-figma-lab/figma-diffs/mui-showcase/rendered.png

── Automatic workflow (do not skip roles) ──
1. Read .cursor/skills/project-orchestrator/SKILL.md — pre-flight: confirm phase + infra + verdict.
2. Read .cursor/skills/roadmap-iteration/SKILL.md — active section: docs/ROADMAP.md (default §1.2 until live strict green).
3. Worker: .cursor/skills/figma-renderer-until-pass/SKILL.md + .cursor/skills/investigate-figma-mismatch/SKILL.md.
4. Fix: code-v2.ts (mock) → plugin build if importer changed.
5. Tier A: re-run sequential steps 1..N for mui--showcase.
6. Tier C if code-v2.ts, scene-to-html.ts, extract.ts, or contract changed.
7. Post-flight: verification-before-completion.
Target: mui--showcase (figma).
── End automatic workflow ──
Read .cursor/skills/figma-renderer-until-pass/SKILL.md and .cursor/skills/investigate-figma-mismatch/SKILL.md — investigate BEFORE editing.
Act immediately — do NOT ask approval to start fixing.
Emulator first until green, then live. Fix now without asking permission.

After fix: pnpm figma:iterate --story mui--showcase

Fix-all iteration 1/5 — THIS STORY ONLY (mui--showcase).
Do not fix other stories in this agent session.
The harness will rebuild the plugin, then re-run figma:iterate for this story.
Goal: PASS on the next automated test (global + hotspot tolerance). WARN/FAIL triggers another attempt.
Do not run the full suite golden yourself — only edit code and rebuild plugin if you changed code-v2.ts.