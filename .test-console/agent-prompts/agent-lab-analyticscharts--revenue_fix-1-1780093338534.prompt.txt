make fixes after live test
Item: lab-analyticscharts--revenue · entry: storybook
Failed test: Storybook → Figma live (figmaLive)
Primary fixer: contract-to-figma
Global: 0.131% · status fail · worst region 1.161%
── Test report (authoritative) ──
Report: /Users/user/Downloads/storybook-to-figma-lab/figma-live-diffs/by-story/lab-analyticscharts-revenue/test-report.json
Original: /Users/user/Downloads/storybook-to-figma-lab/figma-live-diffs/lab-analyticscharts-revenue/storybook.png
Target: /Users/user/Downloads/storybook-to-figma-lab/figma-live-diffs/lab-analyticscharts-revenue/figma.png
Diff: /Users/user/Downloads/storybook-to-figma-lab/figma-live-diffs/lab-analyticscharts-revenue/diff.png
Mismatches: 5 (fix worst first)
Failed test: figmaLive (Storybook → Figma live)
Primary fixer: contract-to-figma
Fixer chain (upstream if contract wrong): storybook-to-contract → contract-to-figma
── Ground rules ──
• Fix the general algorithm — NOT per-item pixel hacks.
• Screenshots are for investigation only — do NOT change UI/code to match PNGs.
• Do NOT embed reference PNGs as assets to pass parity.
── Allowlisted files ──
• packages/figma-importer-plugin/src/code-v2.ts
• packages/pixel-test/src/scene-to-html.ts
── Forbidden ──
• packages/extractor-playwright/**
• packages/pixel-test/src/render-html.ts
• packages/storybook-lab/**
• packages/ui/**
• artifacts/**/*.png
── Mismatch region-01 ──
Region: (268, 268) 88×136px
Wrong pixels: 139 (1.161% in region)
Compare crop: /Users/user/Downloads/storybook-to-figma-lab/figma-live-diffs/lab-analyticscharts-revenue/regions/region-01-compare.png
Verify: pnpm figma:live-iterate --story lab-analyticscharts--revenue
Regression scope: tier-c
Item: lab-analyticscharts--revenue
Verify: pnpm figma:live-iterate --story lab-analyticscharts--revenue
Sandbox: edits in worktree only; regression → auto discard.
Fix-all iteration 1/5 — THIS STORY ONLY (lab-analyticscharts--revenue).
Do not fix other stories in this agent session.
The harness will rebuild the plugin, then re-run live golden for this story.
Goal: PASS on the next automated test (global + hotspot tolerance). WARN/FAIL triggers another attempt.
Do not run the full suite golden yourself — only edit code and rebuild plugin if you changed code-v2.ts.

── Automatic workflow (do not skip roles) ──
1. Read .cursor/skills/project-orchestrator/SKILL.md — pre-flight: confirm phase + infra + verdict.
2. Read .cursor/skills/roadmap-iteration/SKILL.md — active section: docs/ROADMAP.md (default §1.2 until live strict green).
3. Lab memory (.cursor/rules/lab-memory.mdc): Read lab-memory/visual/investigations/active/<storyId>.md (or archive/) if it exists; read linked visual/patterns first.
4. After investigate, before code edits: append lab-memory/templates/investigation.md to the story note.
5. Never store secrets in lab-memory/.
6. Worker: .cursor/skills/figma-renderer-until-pass/SKILL.md + .cursor/skills/investigate-figma-mismatch/SKILL.md — systematic-debugging BEFORE edits.
7. Fix: code-v2.ts → plugin build → YOU run live test (pnpm infra:ensure first). Ask human ready ONLY if bridge/export fails.
8. Regression Tier A: re-run pixel + mock + live for this story (pnpm test:regression -- --tier a --story … --suite figmaLive).
9. Tier C: pnpm test:regression only if you touched shared adapter files.
10. Post-flight: verification-before-completion — paste command exit codes.
Target: lab-analyticscharts--revenue (figmaLive).
── End automatic workflow ──
Read .cursor/skills/figma-renderer-until-pass/SKILL.md and .cursor/skills/investigate-figma-mismatch/SKILL.md — investigate BEFORE editing.
Act immediately — do NOT ask approval to start fixing.
── Linked patterns (apply rule before story-specific hacks) ──
- [[visual/patterns/render-html-computed-font-stack]] — render-html: use computed font stack: Text renders with authored `"Roboto Mono", monospace` while Storybook computed face is `monospace` (common on `<pre>`) — box art misaligns, ~2%+ pixel drift on screen stories.

── Lab memory (prior investigation — finish this, do not re-triage from scratch) ──
Root cause (cached):
  Both Storybook and Figma live render identical content (no missing nodes). Figma mock passes at 0% with the same `artifact.v2.json`, so contract geometry and Inter `computedStack` are correct — **not** the `render-html-computed-font-stack` pattern (stack matches computed here). All five hotspots are **1–2px live-only text placement drift**: legend `strong` percentages ~2px lower (region-01), legend labels ~2px above dots (region-03), h3/h4 horizontal glyph tail drift (regions 02/04), chip badge vertical center off (region-05). Artifact nodes (`lay-15` `43%`: computedBox y=302 h=17 lh=17; parent `legend-row` flex `align:center`) are correct — failure is Figma native text metrics vs Chromium flex centering in `code-v2.ts` live paths.

Recommended fix area (cached):
  `packages/figma-importer-plugin/src/code-v2.ts` — live-only text centering (Tier C): `applyLiveNativeTextBoxCenter`, `liveTightLineHeightPx`, `liveTextPreferWidthAndHeight`, `enforceLiveUnwrappedTextFrame` for flex-row `strong`/span labels, tight line-box headings, and padded `.chip` span. Do **not** touch extractor, `@lab/ui`, or `scene-to-html.ts` unless fixer proves contract wrong. Verify: `pnpm figma:live-iterate --story lab-analyticscharts--revenue`.

Primary fix path (live): packages/figma-importer-plugin/src/code-v2.ts
Use Grep on packages/figma-importer-plugin/src/code-v2.ts — do NOT read code-v2.ts or extract.ts in full before triage.
