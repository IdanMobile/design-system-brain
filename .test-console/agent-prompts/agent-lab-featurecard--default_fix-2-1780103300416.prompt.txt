make fixes after live test

Worst story: lab-featurecard--default (1.58% global, fail, Figma live) · worst hotspot 6.40%
Compare: /Users/user/Downloads/storybook-to-figma-lab/figma-live-diffs/lab-featurecard-default/regions/region-01-compare.png
Storybook: /Users/user/Downloads/storybook-to-figma-lab/figma-live-diffs/lab-featurecard-default/storybook.png
Figma: /Users/user/Downloads/storybook-to-figma-lab/figma-live-diffs/lab-featurecard-default/figma.png

── Triage BEFORE editing renderer code ──
Step 1: open the compare PNG and Storybook + Figma PNGs side-by-side.
Step 2: which side is wrong?
  • Figma missing content that Storybook has  → renderer bug (code-v2.ts / scene-to-html.ts) ✅
  • Figma has content that Storybook is missing  → SOURCE bug (Storybook story, component, asset path, flaky network image). DO NOT edit code-v2.ts.
  • Both look the same but pixels differ  → tolerance, anti-aliasing, font, or sub-pixel layout — usually a small fix in the component or a tolerance config.
Step 3: state your answer (one sentence) before any Read on a renderer file.
Hard limit: do NOT Read code-v2.ts, extract.ts, or figma-live-test.ts in full before completing Step 3. Use Grep for specific symbols if needed.

── Linked patterns (apply rule before story-specific hacks) ──
- [[visual/patterns/figma-guing-screen-roundtrip]] — Figma Guing screen roundtrip (Manifest → Delivery)
- [[visual/patterns/render-html-computed-font-stack]] — render-html: use computed font stack: Text renders with authored `"Roboto Mono", monospace` while Storybook computed face is `monospace` (common on `<pre>`) — box art misaligns, ~2%+ pixel drift on screen stories.

── Lab memory (prior investigation — finish this, do not re-triage from scratch) ──
Root cause (cached):
  **Triage (Step 3):** Both Storybook and Figma show the same content (title, body, uptime stat); pixels differ due to live-only Figma TEXT horizontal positioning / glyph-metric drift vs Chromium — **renderer bug in `code-v2.ts`**, not story source.

  | Section | Storybook | Figma live | Δ | Stage |
  | --- | --- | --- | --- | --- |
  | Title `h3` "Realtime Sync" | Bold 31px, left-aligned | Same glyphs, ~1–2px left shift at crop edge | region-01 3.62% | importer |
  | Body `p` description | 20px grey, wraps 2 lines | Same copy; yellow smear = horizontal line offset | region-01 | importer |
  | Stat `strong` "99.98%" | 42px bold, footer right | Same value; glyph width/edge differs (right fringe) | region-02 1.90% | importer |
  | Icon + card chrome | Match | Match | — | — |

  **Pipeline isolation:**

  - Pixel **pass 0%** — extract + `render-html.ts` correct.
  - Figma mock **pass 0%** — scene JSON / HTML replay correct.
  - Figma live **fail 0.78%** — only Figma plugin TEXT engine path diverges.

  **Artifact trace:** `artifact.v2.json` has correct `text.value`, `box`, `lineHeight`, `font` on `lay-6` (h3), `lay-7` (p), `lay-10` (strong). JSON → mock OK → live wrong ⇒ **`code-v2.ts` live text placement**, not `extract.ts`.

  **Mechanism (grep, not full read):** Live wraps text leaves in frames then runs conflicting placement passes — `blockFlowPinTop` (TOP, y=0) for block `h3`/`p`, then `enforceLiveUnwrappedTextFrame` → `applyLiveNativeTextBoxCenter` (recenters with `textAlignVertical=CENTER` and `liveTextPreferWidthAndHeight`). Footer `strong` uses tight line-box centering while parent flex is `align: baseline`. Figma Inter metrics render slightly wider than Chromium; fixed DOM box width + placement mismatch produces horizontal smear in diff (not missing nodes).

Recommended fix area (cached):
  **Primary:** `packages/figma-importer-plugin/src/code-v2.ts`

  1. **`createTextNode` + frame placement block (~3079–3540):** For block typographic leaves (`h3`, `p`, `strong`) with `liveTextPreferWidthAndHeight`, align live path with mock scene JSON — bare `WIDTH_AND_HEIGHT` at x=0/y=0 inside frame, `clipsContent=false`, avoid post-append recentre that shifts glyphs.
  2. **`enforceLiveUnwrappedTextFrame` / `applyLiveNativeTextBoxCenter`:** Do not override `blockFlowPinTop` TOP pin for `h*`/`p` in block-flow parents; or skip vertical recenter when `text.lineHeight ≈ box.height` (tight line box).
  3. **Footer stat (`strong`, flex `align:baseline`):** Ensure live uses natural glyph width (mock scene: `textAutoResize=WIDTH_AND_HEIGHT`, width ~121px) — do not lock NONE width to DOM box if Figma glyphs exceed it.

  **Do not edit:** Storybook story, `@lab/ui` FeatureCard CSS, or `extract.ts` (artifact already correct).

  **Verify:** `pnpm figma:plugin:build-reload` → reload plugin → `pnpm figma:live-iterate --story lab-featurecard--default` (target global & region ≤0.1%).

Primary fix path (live): packages/figma-importer-plugin/src/code-v2.ts
Use Grep on packages/figma-importer-plugin/src/code-v2.ts — do NOT read code-v2.ts or extract.ts in full before triage.

── Automatic workflow (do not skip roles) ──
1. Read .cursor/skills/project-orchestrator/SKILL.md — pre-flight: confirm phase + infra + verdict.
2. Read .cursor/skills/roadmap-iteration/SKILL.md — active section: docs/ROADMAP.md (default §1.2 until live strict green).
3. Lab memory (.cursor/rules/lab-memory.mdc): Read lab-memory/visual/investigations/active/<storyId>.md (or archive/) if it exists; read linked visual/patterns first.
4. After investigate, before code edits: append lab-memory/templates/investigation.md to the story note.
5. Never store secrets in lab-memory/.
6. Worker: .cursor/skills/figma-renderer-until-pass/SKILL.md + .cursor/skills/investigate-figma-mismatch/SKILL.md — systematic-debugging BEFORE edits.
7. Fix: code-v2.ts → plugin build → YOU run live test (pnpm infra:ensure first). Ask human ready ONLY if bridge/export fails.
8. Regression Tier A: re-run pixel + mock + live for this story (pnpm test:regression -- --tier a --story … --suite figmaLive).
9. Tier C: pnpm test:regression only if you touched shared adapter files.
10. Post-flight: verification-before-completion — paste command exit codes.
Target: lab-featurecard--default (figmaLive).
── End automatic workflow ──
Read .cursor/skills/figma-renderer-until-pass/SKILL.md and .cursor/skills/investigate-figma-mismatch/SKILL.md — investigate BEFORE editing.
Act immediately — do NOT ask approval to start fixing.
Live phase only. Fix now; after rebuild, one-line ask to reload plugin + reply ready before re-live.

After fix: pnpm figma:live-iterate --story lab-featurecard--default

Fix-all iteration 2/5 — THIS STORY ONLY (lab-featurecard--default).
Prior attempt(s) on this story did not reach PASS — read the failure brief below before editing.

── Supervisor mode: NARROW SCOPE ──
Prior attempt went the wrong direction — change ONLY what the supervisor names below.

── Worker supervisor ──
Verdict: WORSE_METRICS · mode: narrow_scope
Supervisor: global diff worsened by 99.22% — revert harmful edits; do not repeat the same approach.
Supervisor: shared adapter file(s) touched — Tier C regression required before marking this story done.
Do not fix other stories in this agent session.
The harness will rebuild the plugin, then re-run live golden for this story.
Goal: PASS on the next automated test (global + hotspot tolerance). WARN/FAIL triggers another attempt.
Do not run the full suite golden yourself — only edit code and rebuild plugin if you changed code-v2.ts.

── Previous attempt did NOT reach PASS (read before changing code) ──
Attempt 1/5 completed without PASS.
Metrics before that fix: fail · 0.78% global · worst hotspot 3.62%
Metrics after automated golden test: error · 100.00% global
Global diff worsened by 99.22% — do not repeat the same approach.
PASS requires global ≤ 0.1% and worst hotspot ≤ 0.1% (both must pass for Figma-style suites).
Report error: Export timed out after 600000ms
Story golden test exited 1.
Last lines from golden test:
exit 1
Compare PNG paths are unchanged but files were regenerated — open the compare image again.
Worst hotspot compare: /Users/user/Downloads/storybook-to-figma-lab/figma-live-diffs/lab-featurecard-default/regions/region-01-compare.png
Artifact: /Users/user/Downloads/storybook-to-figma-lab/figma-live-diffs/lab-featurecard-default/artifact.v2.json
Scene JSON: /Users/user/Downloads/storybook-to-figma-lab/figma-live-diffs/lab-featurecard-default/scene.json