# @lab/story-screen_1

Semantic React delivery for `screen_1` — real `@lab/ui` components (`Screen1`).

Install peer dependency:

```bash
pnpm add @lab/ui
pnpm add ./component.tgz
```

```tsx
import "@lab/ui/styles.css";
import { Screen1, defaultStoryArgs } from "@lab/story-screen_1";

<Screen1 />
```